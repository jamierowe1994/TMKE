{
  "name": "tmke-deliverables-api",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker — R2 upload/download backend for TMKE videography deliverables",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.78.0"
  },
  "dependencies": {
    "@cloudflare/puppeteer": "^1.1.0"
  }
}
