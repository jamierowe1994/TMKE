---
import BaseLayout from "../layouts/BaseLayout.astro";
---

<BaseLayout
  title="The Edit — Template Shop · TMKE"
  description="A curated shop of social media templates designed specifically for the UK property sector. On-brand, easy to customise, ready to post."
>
  <!-- Hero — featured-pack copy on the left, an auto-playing template "film" on the right -->
  <header class="edit-hero">
    <div class="edit-hero-copy">
      <p class="edit-hero-eyebrow">The Edit — Template Shop</p>
      <h1 class="edit-hero-title">The Proof Is in the Posts</h1>
      <p class="edit-hero-desc">Being a great agent means nothing if the people looking for one can't see it. This pack gives you the content to change that. Client testimonials, results, milestones, and service-led posts that build trust, communicate your value, and position you as the agent worth choosing. No guesswork, no generic captions. Just proof.</p>
      <div class="edit-hero-actions">
        <span class="edit-hero-size">Pack Size: 30</span>
        <a class="edit-hero-cta" href="#edit-grid">Discover</a>
      </div>
    </div>
    <div class="edit-hero-film">
      <div class="eh-grid" id="eh-grid" aria-hidden="true">
        {Array.from({ length: 16 }).map(() => (
          <div class="eh-tile"><div class="eh-tile-img"></div></div>
        ))}
      </div>
    </div>
  </header>

  <section class="edit-wrap" id="edit-catalogue" aria-busy="true">
    <div class="edit-loading" id="edit-loading">Loading the catalogue…</div>
    <div class="edit-empty" id="edit-empty" hidden>
      <p>The shop is opening soon — packs will appear here as they're added to The Edit.</p>
    </div>
    <div class="edit-shows" id="edit-grid"></div>
  </section>

  <!-- =========================================================
       Studio demo — a looping ~20s "film" (no real video): a stylised
       recreation of the in-app flow — open a design in the Studio,
       customise it to brand, then schedule it to the TMKE calendar.
       Sits wider than the packs above (page margins, no 1280 cap).
       Pure-CSS choreography; JS only gates playback to on-screen.
       ========================================================= -->
  <section class="studio-ad" aria-labelledby="sad-title">
    <header class="sad-head">
      <p class="sad-eyebrow">More than a shop</p>
      <h2 class="sad-title" id="sad-title">Buy it, make it yours, schedule it.</h2>
      <p class="sad-sub">Every pack opens straight into the Studio. Customise the design to your brand in a few clicks, then drop it onto your TMKE calendar — all without leaving the app.</p>
    </header>

    <figure class="sad-stage" data-studio-demo>
      <!-- fast promo "sizzle": hard cuts flashing through what the Studio does -->
      <div class="sad-reel" aria-hidden="true">
        <span class="sad-wipe sad-anim"></span>
        <span class="sad-mark">TMKE&nbsp;<em>Studio</em></span>

        <!-- ===== Scene 1 — DESIGN ===== -->
        <div class="sad-scene sad-anim" data-s="1">
          <span class="sad-word">Design<b>.</b></span>
          <div class="sad-vis">
            <span class="sad-tpl sad-tpl--1 sad-anim"></span>
            <span class="sad-tpl sad-tpl--2 sad-anim"></span>
            <div class="sad-post sad-anim" data-pop="1">
              <div class="sad-post-img"></div>
              <div class="sad-post-cap"><span>Just Listed</span><b>Marina Heights</b><i>£750,000</i></div>
            </div>
          </div>
        </div>

        <!-- ===== Scene 2 — BRAND ===== -->
        <div class="sad-scene sad-anim" data-s="2">
          <span class="sad-word">Brand<b>.</b></span>
          <div class="sad-vis">
            <div class="sad-chips sad-anim">
              <i style="--c:#371e28"></i><i style="--c:#b9826a"></i><i style="--c:#e6d9c5"></i><i style="--c:#3f9468"></i>
            </div>
            <div class="sad-post sad-post--brand">
              <div class="sad-post-img"></div>
              <span class="sad-post-band sad-anim"></span>
              <div class="sad-post-cap sad-post-cap--brand"><span>Marina Estates</span><b>On&nbsp;brand.</b></div>
            </div>
          </div>
        </div>

        <!-- ===== Scene 3 — RESIZE ===== -->
        <div class="sad-scene sad-anim" data-s="3">
          <span class="sad-word">Resize<b>.</b></span>
          <div class="sad-vis sad-vis--frames">
            <span class="sad-frame sad-frame--1 sad-anim"><em>Post</em></span>
            <span class="sad-frame sad-frame--2 sad-anim"><em>Portrait</em></span>
            <span class="sad-frame sad-frame--3 sad-anim"><em>Story</em></span>
          </div>
        </div>

        <!-- ===== Scene 4 — SCHEDULE ===== -->
        <div class="sad-scene sad-anim" data-s="4">
          <span class="sad-word">Schedule<b>.</b></span>
          <div class="sad-vis sad-vis--cal">
            <div class="sad-week">
              <span></span><span></span><span></span><span class="sad-week-hit"></span><span></span><span></span><span></span>
            </div>
            <span class="sad-stamp sad-anim"></span>
            <span class="sad-tick sad-anim"><b></b>Scheduled</span>
          </div>
        </div>

        <!-- ===== Scene 5 — POST ===== -->
        <div class="sad-scene sad-anim" data-s="5">
          <span class="sad-word">Post<b>.</b></span>
          <div class="sad-vis sad-vis--phone">
            <div class="sad-phone sad-anim">
              <span class="sad-phone-bar"></span>
              <div class="sad-phone-img"></div>
              <span class="sad-phone-foot"></span>
            </div>
            <span class="sad-heart sad-anim"></span>
          </div>
        </div>

        <span class="sad-progress"><i class="sad-anim"></i></span>
      </div>
    </figure>

    <figcaption class="sad-foot">Design, brand, resize and schedule — all in the Studio. Scheduling adds posts to your TMKE calendar; publishing to your socials is coming soon.</figcaption>
  </section>

  <!-- =========================================================
       Checkout modal — three steps: contact → payment → processing
       ========================================================= -->
  <div class="checkout-backdrop" id="checkout" hidden>
    <div class="checkout" role="dialog" aria-modal="true" aria-labelledby="checkout-title">
      <button type="button" class="checkout-close" id="checkout-close" aria-label="Close">&times;</button>

      <aside class="checkout-aside">
        <div class="checkout-aside-cover" id="checkout-cover"></div>
        <div class="checkout-aside-body">
          <div class="checkout-aside-eyebrow">You're buying</div>
          <h3 class="checkout-aside-title" id="checkout-pack-title">Pack title</h3>
          <p class="checkout-aside-desc" id="checkout-pack-desc"></p>
          <ul class="checkout-aside-list" id="checkout-pack-contents"></ul>
          <div class="checkout-aside-foot">
            <span class="checkout-aside-label">Total today</span>
            <span class="checkout-aside-total" id="checkout-pack-price">£299</span>
          </div>
        </div>
      </aside>

      <section class="checkout-main">
        <header class="checkout-steps">
          <span class="checkout-step is-active" data-step-pip="contact">
            <em>01</em> Your details
          </span>
          <span class="checkout-step" data-step-pip="payment">
            <em>02</em> Payment
          </span>
          <span class="checkout-step" data-step-pip="done">
            <em>03</em> Confirmation
          </span>
        </header>

        <!-- Step 1: contact details -->
        <form class="checkout-pane is-active" id="checkout-contact" data-step="contact" novalidate>
          <h2 id="checkout-title" class="checkout-h">Tell us where to send it.</h2>
          <p class="checkout-sub">Your library link and invoice will be emailed to the address below.</p>

          <label class="ck-field">
            <span>Full name</span>
            <input name="name" type="text" required autocomplete="name" />
          </label>
          <label class="ck-field">
            <span>Email</span>
            <input name="email" type="email" required autocomplete="email" />
          </label>
          <div class="ck-row">
            <label class="ck-field">
              <span>Phone</span>
              <input name="phone" type="tel" required autocomplete="tel" />
            </label>
            <label class="ck-field">
              <span>Company / Brand</span>
              <input name="company" type="text" required autocomplete="organization" />
            </label>
          </div>

          <p class="ck-error" id="ck-contact-error" hidden></p>
          <button type="submit" class="ck-cta">Continue to payment <em>&rarr;</em></button>
          <p class="ck-mini">By continuing you agree to our terms. Card details are entered securely on the next step.</p>
        </form>

        <!-- Step 2: payment -->
        <form class="checkout-pane" id="checkout-payment" data-step="payment" novalidate>
          <h2 class="checkout-h">How would you like to pay?</h2>
          <p class="checkout-sub">All transactions are in GBP. You can pay using Apple Pay, Google Pay, or a card.</p>

          <div class="ck-paytabs" role="tablist" aria-label="Payment method">
            <button type="button" class="ck-paytab is-active" data-pay="apple_pay" role="tab" aria-selected="true">
              <span class="ck-paytab-logo" aria-hidden="true"></span>
              <span>Apple Pay</span>
            </button>
            <button type="button" class="ck-paytab" data-pay="google_pay" role="tab" aria-selected="false">
              <span class="ck-paytab-logo ck-paytab-logo--g" aria-hidden="true"></span>
              <span>Google Pay</span>
            </button>
            <button type="button" class="ck-paytab" data-pay="card" role="tab" aria-selected="false">
              <span class="ck-paytab-logo ck-paytab-logo--card" aria-hidden="true"></span>
              <span>Card</span>
            </button>
          </div>

          <!-- Apple / Google Pay panel -->
          <div class="ck-wallet" data-paypane="wallet">
            <div class="ck-wallet-frame">
              <div class="ck-wallet-row">
                <span>Pay TMKE</span>
                <span class="ck-wallet-amount" data-bind="amount">£299</span>
              </div>
              <button type="button" class="ck-wallet-btn" id="ck-wallet-btn">
                <span class="ck-wallet-btn-glyph" aria-hidden="true"></span>
                <span class="ck-wallet-btn-label">Pay with <strong>Apple Pay</strong></span>
              </button>
              <p class="ck-wallet-note">Authentication will appear on your device.</p>
            </div>
          </div>

          <!-- Card panel -->
          <div class="ck-card" data-paypane="card" hidden>
            <label class="ck-field">
              <span>Card number</span>
              <input name="card_number" inputmode="numeric" autocomplete="cc-number" placeholder="1234 1234 1234 1234" maxlength="23" />
            </label>
            <div class="ck-row">
              <label class="ck-field">
                <span>Expiry</span>
                <input name="card_exp" autocomplete="cc-exp" placeholder="MM / YY" maxlength="7" />
              </label>
              <label class="ck-field">
                <span>CVC</span>
                <input name="card_cvc" inputmode="numeric" autocomplete="cc-csc" placeholder="123" maxlength="4" />
              </label>
              <label class="ck-field">
                <span>Postcode</span>
                <input name="card_postcode" autocomplete="postal-code" placeholder="SW1A 1AA" maxlength="8" />
              </label>
            </div>
            <button type="submit" class="ck-cta" id="ck-card-submit">
              Pay <span data-bind="amount">£299</span> <em>&rarr;</em>
            </button>
          </div>

          <p class="ck-error" id="ck-payment-error" hidden></p>
          <button type="button" class="ck-back" id="ck-back-to-contact">&larr; Back to your details</button>
        </form>

        <!-- Step 3: processing / done -->
        <div class="checkout-pane" data-step="done">
          <div class="ck-done">
            <div class="ck-done-spinner" id="ck-done-spinner">
              <span class="ck-done-ring"></span>
              <span class="ck-done-ring"></span>
              <span class="ck-done-ring"></span>
            </div>
            <h2 class="checkout-h" id="ck-done-h">Processing payment…</h2>
            <p class="checkout-sub" id="ck-done-sub">Authenticating with your wallet. Please don't refresh.</p>
          </div>
        </div>
      </section>
    </div>
  </div>

  <style is:global>
    /* The Edit's hero sits further down the page than the default —
       gives the headline more breathing room beneath the nav.
       (Scoped to this page only — Astro emits the style on /edit alone.) */
    /* Two classes needed (.page-hero.wrap) so we win the cascade over
       the global .wrap rule, which uses the shorthand `padding: 0 36px`
       and would otherwise reset padding-top to 0. */
    .page-hero.wrap { padding-top: 200px; }

    /* ===== Hero — small, left-aligned "The Edit" + sub, aligned to the
       catalogue's left edge (same horizontal padding as .edit-wrap). ===== */
    .edit-hero {
      position: relative;
      z-index: 1;
      display: grid;
      grid-template-columns: 1fr 1fr;
      align-items: center;
      gap: clamp(32px, 5vw, 88px);
      max-width: 1320px;
      margin: 0 auto;
      padding: clamp(120px, 15vh, 184px) clamp(24px, 4vw, 64px) clamp(48px, 7vh, 96px);
    }
    @media (max-width: 880px) {
      .edit-hero { grid-template-columns: 1fr; gap: 40px; padding-top: clamp(104px, 14vh, 150px); }
    }
    .edit-hero-copy { max-width: 540px; }
    .edit-hero-eyebrow {
      font-family: var(--sans); font-size: 11px; font-weight: 700;
      letter-spacing: 0.28em; text-transform: uppercase;
      color: var(--english-violet); margin: 0 0 18px;
    }
    .edit-hero-title {
      margin: 0;
      font-family: var(--sans);
      font-weight: 700;
      font-size: clamp(30px, 3.5vw, 50px);
      line-height: 1.08;
      letter-spacing: -0.02em;
      color: var(--ink);
    }
    .edit-hero-desc {
      margin: 22px 0 0;
      max-width: 44ch;
      font-family: var(--sans);
      font-size: clamp(15px, 1.15vw, 16.5px);
      line-height: 1.66;
      color: rgba(28, 29, 34, 0.66);
    }
    .edit-hero-actions {
      display: flex; align-items: center; gap: 16px; flex-wrap: wrap;
      margin-top: clamp(28px, 3.4vw, 40px);
    }
    .edit-hero-size {
      font-family: var(--sans); font-size: 12px; font-weight: 700;
      letter-spacing: 0.14em; text-transform: uppercase; color: var(--ink);
      padding: 11px 18px; border: 1px solid rgba(28, 29, 34, 0.4); border-radius: 2px;
    }
    .edit-hero-cta {
      font-family: var(--sans); font-size: 12px; font-weight: 700;
      letter-spacing: 0.2em; text-transform: uppercase;
      color: var(--paper); background: var(--ink);
      padding: 12px 26px; border-radius: 2px; text-decoration: none;
      transition: background 0.25s, transform 0.4s cubic-bezier(.2,.7,.2,1);
    }
    .edit-hero-cta:hover { background: var(--english-violet); transform: translateY(-1px); }

    /* The "film": a 4×4 grid of template stills that flip in, fill, then fade. */
    .edit-hero-film { display: flex; justify-content: center; }
    .eh-grid {
      display: grid;
      grid-template-columns: repeat(4, 1fr);
      gap: clamp(6px, 0.7vw, 10px);
      width: min(100%, 480px);
      perspective: 1400px;
      transition: opacity 0.9s ease;
    }
    .eh-grid.is-fading { opacity: 0; }
    .eh-tile { position: relative; aspect-ratio: 3 / 4; }
    .eh-tile-img {
      position: absolute; inset: 0;
      background-size: cover; background-position: center;
      background-color: rgba(28, 29, 34, 0.05);
      border-radius: 2px;
      box-shadow: 0 8px 18px -12px rgba(28, 29, 34, 0.45);
      opacity: 0;
      will-change: transform, opacity;
      backface-visibility: hidden;
    }
    .eh-tile.is-on .eh-tile-img { opacity: 1; }
    .eh-tile.is-pop .eh-tile-img { animation: ehPop 0.55s cubic-bezier(.2,.8,.2,1) both; }
    @keyframes ehPop {
      0%   { opacity: 0; transform: rotateY(-78deg) scale(0.86); }
      55%  { opacity: 1; }
      100% { opacity: 1; transform: rotateY(0deg) scale(1); }
    }
    @media (prefers-reduced-motion: reduce) {
      .eh-tile-img { opacity: 1 !important; }
      .eh-tile.is-pop .eh-tile-img { animation: none; }
      .eh-grid.is-fading { opacity: 1; }
    }
    /* ====== STRIPPED — studio demo moved to the homepage Chapter 5 ====== */

    /* Global so the JS-injected catalogue cards inherit the styles —
       Astro scopes by data-attribute which dynamic innerHTML never gets. */
    /* Full-width catalogue — fills the viewport (same side padding as the hero)
       and the 12-col mosaic scales fluidly with the screen size. */
    .edit-wrap {
      position: relative;
      z-index: 1;
      max-width: none;
      margin: 0;
      padding: 0 clamp(24px, 4vw, 64px) 180px;
    }
    @media (max-width: 720px) {
      .edit-wrap { padding: 0 24px 120px; }
    }
    .edit-loading,
    .edit-empty {
      font-family: var(--sans);
      font-size: 14px;
      color: rgba(28,29,34,0.55);
      padding: 32px 0;
    }
    .edit-empty p { max-width: 520px; line-height: 1.6; }

    /* ============ Showcase — alternating feature bands ============
       Each pack is its own editorial "feature": a matted, framed hero print with
       supporting photos clustered around it on one side, and the pack's story +
       price + CTA on the other. Rows alternate (image left / right) and reveal as
       they scroll into view. */
    .edit-shows {
      display: flex;
      flex-direction: column;
      gap: clamp(64px, 9vh, 150px);
      max-width: 1280px;
      margin: 0 auto;
      padding-top: clamp(16px, 4vh, 48px);
    }

    .show { position: relative; }
    .show-inner {
      display: flex;
      align-items: center;
      gap: clamp(28px, 4vw, 76px);
      min-height: min(78vh, 740px);
    }
    /* Three zones across the band: framed hero photo · description (middle) ·
       hoverable preview collage. They alternate sides band to band. */
    .show-hero { flex: 1 1 38%; min-width: 0; order: 1; }
    .show-copy { flex: 1 1 23%; min-width: 0; order: 2; }
    .show-shop { flex: 1 1 39%; min-width: 0; order: 3; }
    .show:nth-child(even) .show-hero { order: 3; }
    .show:nth-child(even) .show-shop { order: 1; }
    @media (max-width: 980px) {
      .show-inner { flex-wrap: wrap; gap: 36px clamp(28px, 4vw, 56px); }
      .show-hero { flex: 1 1 46%; order: 1 !important; }
      .show-copy { flex: 1 1 46%; order: 2 !important; }
      .show-shop { flex: 1 1 100%; order: 3 !important; }
    }
    @media (max-width: 680px) {
      .show-inner { flex-direction: column; min-height: 0; }
      .show-hero, .show-copy, .show-shop { flex: none; width: 100%; }
    }

    /* ---- Framed hero photo ---- */
    .show-hero { position: relative; display: flex; justify-content: center; }
    .show-ghost {
      position: absolute;
      top: -15%; left: -8%;
      font-family: var(--serif); font-weight: 500;
      font-size: clamp(120px, 15vw, 230px); line-height: 1;
      color: rgba(55, 30, 40, 0.06);
      z-index: 0; pointer-events: none; user-select: none;
      translate: 0 var(--par, 0px);
    }
    .show:nth-child(even) .show-ghost { left: auto; right: -8%; }

    .show-stage {
      position: relative; display: block;
      width: min(400px, 94%);
      aspect-ratio: 3 / 4;
      z-index: 1; text-decoration: none;
    }
    /* Gallery-mounted print: paper mount, hairline bevel, layered shadow.
       (Built in CSS — swap for a PNG frame later if you prefer a literal one.) */
    .show-frame {
      position: relative; margin: 0; width: 100%; height: 100%;
      background: var(--paper);
      padding: clamp(12px, 1.5vw, 20px);
      border: 1px solid rgba(28, 29, 34, 0.10);
      box-shadow: 0 40px 70px -30px rgba(28,29,34,0.5), 0 8px 20px -10px rgba(28,29,34,0.28);
      transition: transform 0.7s cubic-bezier(.2,.7,.2,1), box-shadow 0.7s;
    }
    .show-stage:hover .show-frame {
      transform: translateY(-6px);
      box-shadow: 0 54px 92px -34px rgba(28,29,34,0.55), 0 10px 26px -10px rgba(28,29,34,0.3);
    }
    .show-frame::after {
      content: ""; position: absolute; inset: clamp(7px, 1vw, 13px);
      border: 1px solid rgba(28, 29, 34, 0.16); pointer-events: none; z-index: 4;
    }
    .show-mount { position: relative; width: 100%; height: 100%; overflow: hidden; background: var(--platinum); }
    .show-heroimg {
      position: absolute; inset: 0;
      background-size: cover; background-position: center;
      transition: opacity 0.28s ease;
    }
    .show-frame-cap {
      position: absolute; left: 2px; bottom: -28px;
      font-family: var(--sans); font-size: 10px;
      letter-spacing: 0.26em; text-transform: uppercase;
      font-weight: 700; color: rgba(28, 29, 34, 0.45);
    }

    /* ---- Description (middle) ---- */
    .show-copy { position: relative; }
    .show-eyebrow {
      display: flex; align-items: center; gap: 12px;
      font-family: var(--sans); font-size: 11px; font-weight: 700;
      letter-spacing: 0.28em; text-transform: uppercase;
      color: var(--english-violet); margin: 0 0 18px;
    }
    .show-eyebrow::before { content: ""; width: 30px; height: 1px; background: var(--english-violet); opacity: 0.5; }
    .show-title {
      font-family: var(--serif); font-weight: 500;
      font-size: clamp(32px, 3.6vw, 52px);
      line-height: 1.02; letter-spacing: -0.02em;
      margin: 0; color: var(--ink);
    }
    .show-desc {
      font-family: var(--sans); font-size: clamp(15px, 1.15vw, 16.5px);
      line-height: 1.62; color: rgba(28, 29, 34, 0.66); margin: 18px 0 0;
    }
    .show-inside {
      list-style: none; padding: 0; margin: 22px 0 0;
      display: flex; flex-direction: column; gap: 8px;
    }
    .show-inside li {
      font-family: var(--sans); font-size: 13px; line-height: 1.4;
      color: rgba(28, 29, 34, 0.62); padding-left: 19px; position: relative;
    }
    .show-inside li::before { content: "\2192"; position: absolute; left: 0; color: var(--english-violet); }

    /* ---- Preview collage + shop foot ---- */
    .show-shop { position: relative; display: flex; flex-direction: column; }
    .show-collage { display: grid; grid-template-columns: repeat(3, 1fr); gap: 9px; }
    .show-thumb {
      appearance: none; padding: 0; cursor: pointer;
      position: relative; aspect-ratio: 3 / 4;
      background-size: cover; background-position: center;
      background-color: var(--platinum);
      border: 4px solid var(--paper);
      box-shadow: 0 12px 26px -14px rgba(28, 29, 34, 0.45);
      transition: transform 0.45s cubic-bezier(.2,.7,.2,1), box-shadow 0.45s, opacity 0.45s, rotate 0.45s;
    }
    .show-thumb:nth-child(3n+1) { rotate: -2.5deg; }
    .show-thumb:nth-child(3n+2) { rotate: 1.5deg; }
    .show-thumb:nth-child(3n)   { rotate: -1deg; }
    /* Hover one → it pops forward; the rest flicker back. */
    .show-collage:hover .show-thumb { opacity: 0.5; }
    .show-collage .show-thumb:hover,
    .show-collage .show-thumb.is-active {
      opacity: 1; rotate: 0deg;
      transform: translateY(-5px) scale(1.06);
      box-shadow: 0 20px 38px -16px rgba(28, 29, 34, 0.55); z-index: 2;
    }
    .show-shopfoot {
      margin-top: clamp(22px, 2.4vw, 32px);
      display: flex; align-items: center; justify-content: space-between;
      gap: 16px; flex-wrap: wrap;
    }
    .show-price { font-family: var(--serif); font-size: 30px; font-weight: 500; color: var(--ink); }
    .show-price small { font-size: 13px; color: rgba(28, 29, 34, 0.5); }
    .show-ctas { display: flex; align-items: center; gap: 16px; }
    .show-see {
      font-family: var(--sans); font-size: 11px; font-weight: 700;
      letter-spacing: 0.22em; text-transform: uppercase; color: var(--ink);
      text-decoration: none; border-bottom: 1px solid rgba(28, 29, 34, 0.3); padding-bottom: 3px;
      transition: color 0.25s, border-color 0.25s;
    }
    .show-see:hover { color: var(--english-violet); border-color: var(--english-violet); }
    .show-buy {
      appearance: none; background: var(--ink); color: var(--paper);
      border: 0; padding: 13px 22px; border-radius: 2px;
      font-family: var(--sans); font-size: 11px; font-weight: 700;
      letter-spacing: 0.24em; text-transform: uppercase; cursor: pointer;
      transition: background 0.3s, transform 0.4s cubic-bezier(.2,.7,.2,1);
    }
    .show-buy:hover { background: var(--english-violet); transform: translateX(2px); }
    .show-buy em { font-family: var(--serif); font-style: italic; font-size: 15px; letter-spacing: 0; text-transform: none; margin-left: 8px; }

    /* ---- Scroll-in reveal ---- */
    .show-hero, .show-copy, .show-shop {
      opacity: 0; transform: translateY(34px);
      transition: opacity 1s cubic-bezier(.2,.7,.2,1), transform 1s cubic-bezier(.2,.7,.2,1);
    }
    .show.is-in .show-hero { opacity: 1; transform: none; transition-delay: 0.04s; }
    .show.is-in .show-copy { opacity: 1; transform: none; transition-delay: 0.14s; }
    .show.is-in .show-shop { opacity: 1; transform: none; transition-delay: 0.22s; }
    @media (prefers-reduced-motion: reduce) {
      .show-hero, .show-copy, .show-shop { opacity: 1 !important; transform: none !important; }
      .show-frame, .show-thumb, .show-heroimg { transition: none; }
    }

    /* ============ Checkout modal ============ */
    .checkout-backdrop[hidden] { display: none !important; }
    .checkout-backdrop {
      position: fixed; inset: 0;
      background: rgba(28, 29, 34, 0.65);
      z-index: 100;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 24px;
      overflow-y: auto;
    }
    .checkout {
      position: relative;
      width: min(1080px, 100%);
      max-height: calc(100vh - 48px);
      display: grid;
      grid-template-columns: 360px 1fr;
      background: var(--paper);
      border-radius: 4px;
      overflow: hidden;
      box-shadow: 0 40px 100px rgba(0,0,0,0.35);
    }
    @media (max-width: 860px) {
      .checkout { grid-template-columns: 1fr; max-height: calc(100vh - 48px); overflow-y: auto; }
    }
    .checkout-close {
      position: absolute;
      top: 14px; right: 16px;
      background: rgba(244, 242, 241,0.85);
      border: 1px solid rgba(var(--violet-rgb),0.2);
      border-radius: 999px;
      width: 36px; height: 36px;
      font-size: 22px;
      color: var(--ink);
      cursor: pointer;
      z-index: 5;
      line-height: 1;
    }
    .checkout-close:hover { background: var(--paper); }

    .checkout-aside {
      background: linear-gradient(180deg, var(--english-violet), #2f2b39);
      color: var(--paper);
      display: flex;
      flex-direction: column;
    }
    .checkout-aside-cover {
      aspect-ratio: 4 / 3;
      background-size: cover;
      background-position: center;
      background-color: var(--platinum);
    }
    .checkout-aside-body { padding: 28px 28px 32px; display: flex; flex-direction: column; gap: 10px; flex: 1; }
    .checkout-aside-eyebrow {
      font-family: var(--sans);
      font-size: 11px;
      letter-spacing: 0.28em;
      text-transform: uppercase;
      font-weight: 700;
      color: rgba(244, 242, 241,0.6);
    }
    .checkout-aside-title {
      font-family: var(--serif);
      font-size: 30px;
      font-weight: 500;
      line-height: 1.05;
      letter-spacing: -0.015em;
      color: var(--paper);
      margin: 0;
    }
    .checkout-aside-desc {
      font-size: 14px;
      line-height: 1.55;
      color: rgba(244, 242, 241,0.7);
      margin: 6px 0 4px;
    }
    .checkout-aside-list {
      list-style: none; padding: 0; margin: 12px 0 0;
      display: flex; flex-direction: column; gap: 6px;
      font-size: 13px;
      color: rgba(244, 242, 241,0.75);
    }
    .checkout-aside-list li { padding-left: 18px; position: relative; }
    .checkout-aside-list li::before {
      content: "—";
      position: absolute; left: 0; color: rgba(244, 242, 241,0.45);
    }
    .checkout-aside-foot {
      margin-top: auto;
      padding-top: 22px;
      border-top: 1px solid rgba(244, 242, 241,0.18);
      display: flex; justify-content: space-between; align-items: baseline;
    }
    .checkout-aside-label {
      font-family: var(--sans);
      font-size: 11px;
      letter-spacing: 0.24em;
      text-transform: uppercase;
      font-weight: 700;
      color: rgba(244, 242, 241,0.6);
    }
    .checkout-aside-total {
      font-family: var(--serif);
      font-size: 36px;
      font-weight: 500;
      color: var(--paper);
    }

    .checkout-main {
      padding: 40px 44px 44px;
      display: flex;
      flex-direction: column;
      overflow-y: auto;
    }
    @media (max-width: 860px) { .checkout-main { padding: 32px 24px 32px; } }

    .checkout-steps {
      display: flex;
      gap: 22px;
      margin-bottom: 26px;
      padding-bottom: 18px;
      border-bottom: 1px solid rgba(28,29,34,0.1);
      font-family: var(--sans);
      font-size: 11px;
      letter-spacing: 0.24em;
      text-transform: uppercase;
      font-weight: 700;
      color: rgba(28,29,34,0.4);
    }
    .checkout-step { display: inline-flex; align-items: center; gap: 8px; }
    .checkout-step em {
      font-family: var(--serif);
      font-style: italic;
      font-size: 16px;
      letter-spacing: 0;
      color: rgba(28,29,34,0.3);
    }
    .checkout-step.is-active { color: var(--english-violet); }
    .checkout-step.is-active em { color: var(--english-violet); }
    .checkout-step.is-done { color: var(--ink); }

    .checkout-pane { display: none; flex-direction: column; gap: 16px; }
    .checkout-pane.is-active { display: flex; }
    .checkout-h {
      font-family: var(--serif);
      font-size: clamp(28px, 3vw, 38px);
      font-weight: 500;
      letter-spacing: -0.015em;
      line-height: 1.1;
      margin: 0;
    }
    .checkout-sub {
      font-size: 14px;
      line-height: 1.55;
      color: rgba(28,29,34,0.65);
      margin: -6px 0 12px;
    }

    .ck-field { display: flex; flex-direction: column; gap: 6px; }
    .ck-field > span {
      font-family: var(--sans);
      font-size: 11px;
      font-weight: 700;
      letter-spacing: 0.22em;
      text-transform: uppercase;
      color: var(--english-violet);
    }
    .ck-field input {
      font-family: var(--sans);
      font-size: 15px;
      padding: 12px 14px;
      border: 1px solid rgba(var(--violet-rgb),0.28);
      border-radius: 2px;
      background: #fff;
      color: var(--ink);
      transition: border-color 0.25s, box-shadow 0.25s;
    }
    .ck-field input:focus {
      outline: none;
      border-color: var(--english-violet);
      box-shadow: 0 0 0 3px rgba(var(--violet-rgb),0.12);
    }
    .ck-row {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: 14px;
    }
    .ck-row:has(> :nth-child(2):last-child) { grid-template-columns: 1fr 1fr; }
    @media (max-width: 640px) { .ck-row { grid-template-columns: 1fr; } }

    .ck-cta {
      appearance: none;
      background: var(--ink);
      color: var(--paper);
      border: 0;
      padding: 16px 22px;
      border-radius: 2px;
      font-family: var(--sans);
      font-size: 12px;
      font-weight: 700;
      letter-spacing: 0.24em;
      text-transform: uppercase;
      cursor: pointer;
      margin-top: 10px;
      transition: background 0.25s;
    }
    .ck-cta:hover { background: var(--english-violet); }
    .ck-cta em {
      font-family: var(--serif);
      font-style: italic;
      font-size: 16px;
      letter-spacing: 0;
      text-transform: none;
      margin-left: 8px;
    }
    .ck-cta[disabled] { opacity: 0.55; cursor: progress; }

    .ck-back {
      background: transparent;
      border: 0;
      color: rgba(28,29,34,0.55);
      font-family: var(--sans);
      font-size: 12px;
      letter-spacing: 0.2em;
      text-transform: uppercase;
      font-weight: 700;
      padding: 6px 0;
      margin-top: 6px;
      cursor: pointer;
      align-self: flex-start;
    }
    .ck-back:hover { color: var(--english-violet); }
    .ck-mini { font-size: 11px; color: rgba(28,29,34,0.45); margin: 6px 0 0; line-height: 1.5; }
    .ck-error {
      margin: 0;
      padding: 10px 12px;
      background: rgba(160,90,60,0.1);
      border-left: 3px solid #a05a3c;
      color: #a05a3c;
      font-size: 13px;
      font-family: var(--sans);
    }

    /* Payment tabs */
    .ck-paytabs {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: 10px;
      margin: 4px 0 12px;
    }
    @media (max-width: 520px) { .ck-paytabs { grid-template-columns: 1fr; } }
    .ck-paytab {
      appearance: none;
      background: #fff;
      border: 1px solid rgba(var(--violet-rgb),0.25);
      border-radius: 2px;
      padding: 14px 14px;
      display: inline-flex;
      align-items: center;
      gap: 10px;
      font-family: var(--sans);
      font-size: 13px;
      font-weight: 700;
      letter-spacing: 0.04em;
      color: var(--ink);
      cursor: pointer;
      transition: border-color 0.25s, background 0.25s;
    }
    .ck-paytab:hover { border-color: var(--english-violet); }
    .ck-paytab.is-active {
      border-color: var(--ink);
      background: var(--ink);
      color: var(--paper);
    }
    .ck-paytab-logo {
      width: 22px; height: 22px;
      background: #000;
      mask-image: url("data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 24 24'><path d='M16.4 12.6c0-2.7 2.2-3.9 2.3-4-1.2-1.8-3.2-2-3.9-2-1.6-.2-3.2 1-4.1 1-.9 0-2.2-1-3.6-1-1.9 0-3.6 1.1-4.6 2.8-2 3.4-.5 8.4 1.4 11.1.9 1.3 2 2.8 3.4 2.8 1.4-.1 1.9-.9 3.6-.9 1.7 0 2.1.9 3.6.9 1.5 0 2.4-1.3 3.3-2.7 1-1.5 1.5-3 1.5-3 0-.1-2.9-1.1-2.9-4z M14.1 4.4c.7-.9 1.2-2.1 1.1-3.3-1 0-2.3.7-3 1.5-.7.8-1.3 2-1.1 3.2 1.2 0 2.4-.6 3-1.4z'/></svg>");
      -webkit-mask-image: url("data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 24 24'><path d='M16.4 12.6c0-2.7 2.2-3.9 2.3-4-1.2-1.8-3.2-2-3.9-2-1.6-.2-3.2 1-4.1 1-.9 0-2.2-1-3.6-1-1.9 0-3.6 1.1-4.6 2.8-2 3.4-.5 8.4 1.4 11.1.9 1.3 2 2.8 3.4 2.8 1.4-.1 1.9-.9 3.6-.9 1.7 0 2.1.9 3.6.9 1.5 0 2.4-1.3 3.3-2.7 1-1.5 1.5-3 1.5-3 0-.1-2.9-1.1-2.9-4z M14.1 4.4c.7-.9 1.2-2.1 1.1-3.3-1 0-2.3.7-3 1.5-.7.8-1.3 2-1.1 3.2 1.2 0 2.4-.6 3-1.4z'/></svg>");
      mask-size: contain; -webkit-mask-size: contain;
      mask-repeat: no-repeat; -webkit-mask-repeat: no-repeat;
    }
    .ck-paytab.is-active .ck-paytab-logo { background: var(--paper); }
    .ck-paytab-logo--g {
      /* Letter G in the same monochrome treatment */
      mask: none; -webkit-mask: none;
      background: transparent;
      display: inline-flex; align-items: center; justify-content: center;
      font-family: var(--sans); font-weight: 800; font-size: 16px;
      color: inherit;
    }
    .ck-paytab-logo--g::before { content: "G"; }
    .ck-paytab-logo--card {
      mask: none; -webkit-mask: none;
      background: transparent;
      border: 1.6px solid currentColor;
      border-radius: 3px;
      position: relative;
    }
    .ck-paytab-logo--card::after {
      content: ""; position: absolute; left: 1px; right: 1px; top: 6px;
      height: 3px; background: currentColor;
    }

    /* Wallet pane */
    .ck-wallet { margin: 6px 0 14px; }
    .ck-wallet-frame {
      border: 1px solid rgba(var(--violet-rgb),0.2);
      border-radius: 4px;
      padding: 22px;
      background: #fff;
      display: flex; flex-direction: column; gap: 16px;
    }
    .ck-wallet-row {
      display: flex; justify-content: space-between; align-items: baseline;
      font-family: var(--sans);
      font-size: 13px;
      letter-spacing: 0.18em;
      text-transform: uppercase;
      font-weight: 600;
      color: rgba(28,29,34,0.55);
    }
    .ck-wallet-amount {
      font-family: var(--serif);
      font-size: 28px;
      font-weight: 500;
      letter-spacing: 0;
      text-transform: none;
      color: var(--ink);
    }
    .ck-wallet-btn {
      appearance: none;
      background: #000;
      color: #fff;
      border: 0;
      border-radius: 6px;
      padding: 16px 22px;
      font-family: -apple-system, "Helvetica Neue", var(--sans);
      font-size: 16px;
      font-weight: 600;
      letter-spacing: 0.01em;
      cursor: pointer;
      display: inline-flex; align-items: center; justify-content: center; gap: 10px;
      transition: background 0.25s, transform 0.25s;
    }
    .ck-wallet-btn:hover { background: #1a1a1a; transform: translateY(-1px); }
    .ck-wallet-btn-glyph {
      width: 18px; height: 22px;
      background: #fff;
      mask-image: url("data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 24 24'><path d='M16.4 12.6c0-2.7 2.2-3.9 2.3-4-1.2-1.8-3.2-2-3.9-2-1.6-.2-3.2 1-4.1 1-.9 0-2.2-1-3.6-1-1.9 0-3.6 1.1-4.6 2.8-2 3.4-.5 8.4 1.4 11.1.9 1.3 2 2.8 3.4 2.8 1.4-.1 1.9-.9 3.6-.9 1.7 0 2.1.9 3.6.9 1.5 0 2.4-1.3 3.3-2.7 1-1.5 1.5-3 1.5-3 0-.1-2.9-1.1-2.9-4z M14.1 4.4c.7-.9 1.2-2.1 1.1-3.3-1 0-2.3.7-3 1.5-.7.8-1.3 2-1.1 3.2 1.2 0 2.4-.6 3-1.4z'/></svg>");
      -webkit-mask-image: url("data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 24 24'><path d='M16.4 12.6c0-2.7 2.2-3.9 2.3-4-1.2-1.8-3.2-2-3.9-2-1.6-.2-3.2 1-4.1 1-.9 0-2.2-1-3.6-1-1.9 0-3.6 1.1-4.6 2.8-2 3.4-.5 8.4 1.4 11.1.9 1.3 2 2.8 3.4 2.8 1.4-.1 1.9-.9 3.6-.9 1.7 0 2.1.9 3.6.9 1.5 0 2.4-1.3 3.3-2.7 1-1.5 1.5-3 1.5-3 0-.1-2.9-1.1-2.9-4z M14.1 4.4c.7-.9 1.2-2.1 1.1-3.3-1 0-2.3.7-3 1.5-.7.8-1.3 2-1.1 3.2 1.2 0 2.4-.6 3-1.4z'/></svg>");
      mask-size: contain; -webkit-mask-size: contain;
      mask-repeat: no-repeat; -webkit-mask-repeat: no-repeat;
    }
    .ck-wallet-btn[data-wallet="google_pay"] {
      background: #fff;
      color: #3c4043;
      border: 1px solid #dadce0;
    }
    .ck-wallet-btn[data-wallet="google_pay"] .ck-wallet-btn-glyph {
      mask: none; -webkit-mask: none; background: transparent;
      display: inline-flex; align-items: center; justify-content: center;
      width: auto; height: auto;
      font-weight: 700; font-size: 18px;
    }
    .ck-wallet-btn[data-wallet="google_pay"] .ck-wallet-btn-glyph::before {
      content: "G"; color: #4285F4;
    }
    .ck-wallet-note { font-size: 12px; color: rgba(28,29,34,0.5); margin: 0; text-align: center; }

    .ck-card { display: flex; flex-direction: column; gap: 14px; }
    .ck-card[hidden],
    .ck-wallet[hidden] { display: none !important; }

    /* Done pane */
    .ck-done {
      padding: 40px 0;
      display: flex; flex-direction: column; align-items: center; text-align: center; gap: 18px;
    }
    .ck-done-spinner {
      position: relative; width: 70px; height: 70px; margin-bottom: 8px;
    }
    .ck-done-ring {
      position: absolute; inset: 0;
      border: 2px solid transparent;
      border-top-color: var(--english-violet);
      border-radius: 50%;
      animation: ck-spin 1.4s cubic-bezier(.5,.1,.5,.9) infinite;
    }
    .ck-done-ring:nth-child(2) { inset: 10px; border-top-color: rgba(var(--violet-rgb),0.4); animation-duration: 1.7s; animation-direction: reverse; }
    .ck-done-ring:nth-child(3) { inset: 20px; border-top-color: rgba(var(--violet-rgb),0.2); animation-duration: 2.1s; }
    @keyframes ck-spin { from { transform: rotate(0); } to { transform: rotate(360deg); } }

    /* =========================================================
       Studio demo — looping ~20s "film" (no real video). Wider than the
       1280 packs above; uses the page's side margins. Pulled up under the
       catalogue. Pure-CSS timeline: every animated element shares a 20s
       infinite loop, cued by % stops, and returns to its start state by
       100% for a seamless loop. Playback is gated to on-screen via JS.
       ========================================================= */
    .studio-ad {
      position: relative; z-index: 1;
      max-width: 1680px;
      margin: clamp(-150px, -9vw, -60px) auto 0;
      padding: 0 clamp(24px, 4vw, 64px) clamp(96px, 14vh, 180px);
    }
    .sad-head { max-width: 760px; margin: 0 auto clamp(28px, 4vh, 52px); text-align: center; }
    .sad-eyebrow {
      font-family: var(--sans); font-size: 11px; font-weight: 700;
      letter-spacing: 0.28em; text-transform: uppercase;
      color: var(--english-violet); margin: 0 0 16px;
    }
    .sad-title {
      font-family: var(--serif); font-weight: 500;
      font-size: clamp(30px, 4vw, 52px); line-height: 1.04;
      letter-spacing: -0.02em; color: var(--ink); margin: 0;
    }
    .sad-sub {
      font-family: var(--sans); font-size: clamp(15px, 1.2vw, 17px);
      line-height: 1.6; color: rgba(28, 29, 34, 0.62);
      margin: 18px auto 0; max-width: 56ch;
    }

    /* ---- promo reel (no monitor) — fast hard-cut sizzle ---- */
    .sad-stage { margin: 0; position: relative; }
    .sad-reel {
      position: relative; width: 100%; max-width: 1560px; margin: 0 auto;
      aspect-ratio: 2.3 / 1; max-height: 74vh; overflow: hidden;
      border-radius: clamp(8px, 1vw, 16px);
      background:
        radial-gradient(120% 140% at 10% -10%, rgba(185,130,106,0.18), transparent 52%),
        radial-gradient(120% 140% at 105% 110%, rgba(55,30,40,0.14), transparent 52%),
        #faf8f5;
      box-shadow: 0 40px 80px -40px rgba(28,29,34,0.45), 0 12px 28px -20px rgba(28,29,34,0.32);
      font-family: var(--sans); color: var(--ink); isolation: isolate;
    }
    @media (max-width: 720px) { .sad-reel { aspect-ratio: 4 / 5; } }

    /* transition wipe that sweeps across on every cut */
    .sad-wipe {
      position: absolute; top: -12%; bottom: -12%; left: -42%; width: 34%; z-index: 9;
      background: linear-gradient(90deg, transparent, var(--english-violet) 40%, var(--accent) 70%, transparent);
      transform: skewX(-12deg); pointer-events: none;
    }

    /* persistent wordmark */
    .sad-mark { position: absolute; top: clamp(12px, 2.2vw, 26px); left: clamp(14px, 2.4vw, 32px); z-index: 8; font-size: clamp(11px, 1.05vw, 15px); font-weight: 700; letter-spacing: 0.01em; color: var(--ink); }
    .sad-mark em { font-style: normal; color: var(--english-violet); }

    /* each scene fills the reel; hard cuts between them */
    .sad-scene {
      position: absolute; inset: 0; display: grid; grid-template-columns: 1fr 1fr;
      align-items: center; gap: clamp(16px, 3vw, 54px);
      padding: clamp(26px, 4vw, 70px) clamp(30px, 5vw, 86px); opacity: 0;
    }
    @media (max-width: 720px) { .sad-scene { grid-template-columns: 1fr; gap: 20px; text-align: center; justify-items: center; } }

    /* the big kinetic word */
    .sad-word { font-family: var(--serif); font-weight: 600; font-size: clamp(38px, 8vw, 118px); line-height: 0.9; letter-spacing: -0.03em; color: var(--ink); white-space: nowrap; }
    .sad-word b { color: var(--accent); }

    /* the visual half */
    .sad-vis { position: relative; display: flex; align-items: center; justify-content: center; min-width: 0; height: 100%; }

    /* S1 — post card + two templates fanned behind */
    .sad-post { position: relative; z-index: 2; width: min(60%, 220px); aspect-ratio: 4 / 5; border-radius: 8px; overflow: hidden; background: #fff; box-shadow: 0 26px 48px -22px rgba(28,29,34,0.5); display: flex; flex-direction: column; }
    .sad-post-img { flex: 1; min-height: 0; background: url("/assets/home%20window.jpg") center/cover; }
    .sad-post-cap { padding: clamp(7px, 0.9vw, 13px); background: #fff; }
    .sad-post-cap span { display: block; font-size: clamp(6.5px, 0.66vw, 9.5px); font-weight: 700; letter-spacing: 0.2em; text-transform: uppercase; color: var(--accent); }
    .sad-post-cap b { display: block; font-family: var(--subhead); font-weight: 500; font-size: clamp(12px, 1.5vw, 21px); line-height: 1.04; color: var(--ink); margin: 2px 0; }
    .sad-post-cap i { font-style: normal; font-size: clamp(7.5px, 0.78vw, 11px); font-weight: 600; color: rgba(28,29,34,0.55); }
    .sad-tpl { position: absolute; top: 50%; width: min(44%, 168px); aspect-ratio: 4 / 5; border-radius: 7px; background-size: cover; background-position: center; box-shadow: 0 16px 32px -18px rgba(28,29,34,0.5); z-index: 1; }
    .sad-tpl--1 { right: 55%; background-image: url("/assets/cosy%20home.jpg"); transform: translateY(-50%) rotate(-8deg); }
    .sad-tpl--2 { left: 55%; background-image: url("/assets/sitting%20chair.jpg"); transform: translateY(-50%) rotate(8deg); }

    /* S2 — brand chips + recoloring band */
    .sad-chips { position: absolute; left: 2%; top: 12%; display: flex; gap: clamp(6px, 0.8vw, 12px); z-index: 3; }
    .sad-chips i { width: clamp(16px, 2.3vw, 36px); aspect-ratio: 1; border-radius: 50%; background: var(--c); box-shadow: 0 8px 18px -8px rgba(28,29,34,0.5); }
    .sad-post--brand { width: min(56%, 200px); }
    .sad-post-band { position: absolute; left: 0; right: 0; bottom: 26%; height: 11%; background: var(--english-violet); z-index: 3; }
    .sad-post-cap--brand b { color: var(--english-violet); }

    /* S3 — resize frames */
    .sad-vis--frames { gap: clamp(12px, 1.8vw, 30px); align-items: center; }
    .sad-frame { position: relative; border: 2.5px solid var(--english-violet); border-radius: 6px; background: rgba(185,130,106,0.12); }
    .sad-frame em { position: absolute; left: 50%; bottom: -1.7em; transform: translateX(-50%); font-style: normal; font-size: clamp(6.5px, 0.78vw, 11px); font-weight: 700; letter-spacing: 0.1em; text-transform: uppercase; color: rgba(28,29,34,0.55); white-space: nowrap; }
    .sad-frame--1 { width: clamp(44px, 6.6vw, 104px); aspect-ratio: 1; }
    .sad-frame--2 { width: clamp(38px, 5.6vw, 90px); aspect-ratio: 4 / 5; }
    .sad-frame--3 { width: clamp(28px, 4.2vw, 66px); aspect-ratio: 9 / 16; }

    /* S4 — week + dropping stamp + scheduled tick */
    .sad-vis--cal { flex-direction: column; gap: clamp(14px, 2vw, 26px); }
    .sad-week { position: relative; display: grid; grid-template-columns: repeat(7, 1fr); gap: clamp(5px, 0.7vw, 10px); width: min(100%, 380px); }
    .sad-week span { aspect-ratio: 3 / 4; border-radius: 6px; background: rgba(28,29,34,0.06); }
    .sad-week-hit { box-shadow: inset 0 0 0 2px var(--english-violet); }
    .sad-stamp { position: absolute; left: 50%; top: 0; width: clamp(34px, 5vw, 64px); aspect-ratio: 4 / 5; border-radius: 5px; background: url("/assets/home%20window.jpg") center/cover; border: 3px solid #fff; box-shadow: 0 16px 30px -12px rgba(28,29,34,0.5); z-index: 4; opacity: 0; }
    .sad-tick { display: inline-flex; align-items: center; gap: 7px; background: var(--english-violet); color: #fff; font-size: clamp(9px, 1vw, 13px); font-weight: 700; padding: 0.55em 1em; border-radius: 999px; opacity: 0; }
    .sad-tick b { width: 1.1em; height: 1.1em; border-radius: 50%; background: #4caf72; position: relative; flex: 0 0 auto; }
    .sad-tick b::after { content: ""; position: absolute; left: 35%; top: 18%; width: 26%; height: 50%; border: solid #fff; border-width: 0 2px 2px 0; transform: rotate(45deg); }

    /* S5 — phone with the post */
    .sad-vis--phone { gap: 0; }
    .sad-phone { position: relative; width: clamp(86px, 12vw, 168px); aspect-ratio: 9 / 18; border-radius: clamp(12px, 1.4vw, 22px); background: #1c1d22; padding: 6%; box-shadow: 0 30px 56px -24px rgba(28,29,34,0.55); display: flex; flex-direction: column; gap: 5%; opacity: 0; }
    .sad-phone-bar { height: 6%; background: rgba(255,255,255,0.85); border-radius: 3px; }
    .sad-phone-img { flex: 1; min-height: 0; border-radius: 6px; background: url("/assets/home%20window.jpg") center/cover; }
    .sad-phone-foot { height: 8%; background: rgba(255,255,255,0.85); border-radius: 3px; }
    .sad-heart { position: absolute; right: 12%; bottom: 24%; width: clamp(20px, 2.6vw, 38px); aspect-ratio: 1; z-index: 5; background: var(--accent); opacity: 0;
      -webkit-mask: url("data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 24 24'><path d='M12 21s-8-5.3-8-11a4.5 4.5 0 0 1 8-2.8A4.5 4.5 0 0 1 20 10c0 5.7-8 11-8 11z'/></svg>") center/contain no-repeat;
      mask: url("data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 24 24'><path d='M12 21s-8-5.3-8-11a4.5 4.5 0 0 1 8-2.8A4.5 4.5 0 0 1 20 10c0 5.7-8 11-8 11z'/></svg>") center/contain no-repeat;
    }

    /* loop progress hairline */
    .sad-progress { position: absolute; left: 0; right: 0; bottom: 0; height: 3px; background: rgba(28,29,34,0.08); z-index: 10; overflow: hidden; }
    .sad-progress i { display: block; height: 100%; width: 0; background: linear-gradient(90deg, var(--english-violet), var(--accent)); }

    /* ---------- the 8s timeline — fast hard cuts, every element shares it ---------- */
    .sad-anim { animation-duration: 8s; animation-iteration-count: infinite; animation-timing-function: ease-out; }
    .sad-wipe { animation-name: sadWipe; animation-duration: 1.6s; animation-timing-function: cubic-bezier(.7,0,.3,1); }
    [data-s="1"] { animation-name: sadSc1; }
    [data-s="2"] { animation-name: sadSc2; }
    [data-s="3"] { animation-name: sadSc3; }
    [data-s="4"] { animation-name: sadSc4; }
    [data-s="5"] { animation-name: sadSc5; }
    [data-pop="1"] { animation-name: sadPop; animation-timing-function: cubic-bezier(.3,1.4,.5,1); }
    .sad-tpl--1 { animation-name: sadTplA; }
    .sad-tpl--2 { animation-name: sadTplB; }
    .sad-chips { animation-name: sadChips; }
    .sad-post-band { animation-name: sadBand; animation-timing-function: steps(1, end); }
    .sad-frame--1 { animation-name: sadFrA; animation-timing-function: cubic-bezier(.3,1.6,.5,1); }
    .sad-frame--2 { animation-name: sadFrB; animation-timing-function: cubic-bezier(.3,1.6,.5,1); }
    .sad-frame--3 { animation-name: sadFrC; animation-timing-function: cubic-bezier(.3,1.6,.5,1); }
    .sad-stamp { animation-name: sadStamp; animation-timing-function: cubic-bezier(.4,1.3,.5,1); }
    .sad-tick { animation-name: sadTick; animation-timing-function: cubic-bezier(.3,1.5,.5,1); }
    .sad-phone { animation-name: sadPhone; animation-timing-function: cubic-bezier(.3,1.3,.5,1); }
    .sad-heart { animation-name: sadHeart; animation-timing-function: cubic-bezier(.3,1.7,.5,1); }
    .sad-progress i { animation-name: sadProg; animation-timing-function: linear; }

    @keyframes sadWipe {
      0%   { transform: translateX(0) skewX(-12deg); opacity: 0; }
      8%   { opacity: 0.95; }
      42%  { transform: translateX(430%) skewX(-12deg); opacity: 0.95; }
      48%,100% { transform: translateX(430%) skewX(-12deg); opacity: 0; }
    }
    @keyframes sadSc1 {
      0%   { opacity: 1; transform: translateX(6%); }
      3%   { opacity: 1; transform: none; }
      17%  { opacity: 1; transform: none; }
      19%  { opacity: 0; transform: translateX(-6%); }
      100% { opacity: 0; transform: translateX(-6%); }
    }
    @keyframes sadSc2 {
      0%   { opacity: 0; transform: translateX(7%); }
      20%  { opacity: 0; transform: translateX(7%); }
      20.01% { opacity: 1; }
      23%  { opacity: 1; transform: none; }
      37%  { opacity: 1; transform: none; }
      39%  { opacity: 0; transform: translateX(-6%); }
      100% { opacity: 0; transform: translateX(-6%); }
    }
    @keyframes sadSc3 {
      0%   { opacity: 0; transform: translateX(7%); }
      40%  { opacity: 0; transform: translateX(7%); }
      40.01% { opacity: 1; }
      43%  { opacity: 1; transform: none; }
      57%  { opacity: 1; transform: none; }
      59%  { opacity: 0; transform: translateX(-6%); }
      100% { opacity: 0; transform: translateX(-6%); }
    }
    @keyframes sadSc4 {
      0%   { opacity: 0; transform: translateX(7%); }
      60%  { opacity: 0; transform: translateX(7%); }
      60.01% { opacity: 1; }
      63%  { opacity: 1; transform: none; }
      77%  { opacity: 1; transform: none; }
      79%  { opacity: 0; transform: translateX(-6%); }
      100% { opacity: 0; transform: translateX(-6%); }
    }
    @keyframes sadSc5 {
      0%   { opacity: 0; transform: translateX(7%); }
      80%  { opacity: 0; transform: translateX(7%); }
      80.01% { opacity: 1; }
      83%,100% { opacity: 1; transform: none; }
    }
    @keyframes sadPop {
      0%   { opacity: 0; transform: scale(0.6) rotate(-3deg); }
      4%   { opacity: 1; transform: scale(1.05) rotate(0); }
      7%,100% { opacity: 1; transform: scale(1); }
    }
    @keyframes sadTplA {
      0%   { opacity: 0; transform: translate(55%, -50%) rotate(0); }
      6%   { opacity: 1; transform: translateY(-50%) rotate(-8deg); }
      100% { opacity: 1; transform: translateY(-50%) rotate(-8deg); }
    }
    @keyframes sadTplB {
      0%   { opacity: 0; transform: translate(-55%, -50%) rotate(0); }
      6%   { opacity: 1; transform: translateY(-50%) rotate(8deg); }
      100% { opacity: 1; transform: translateY(-50%) rotate(8deg); }
    }
    @keyframes sadChips {
      0%,20%   { opacity: 0; transform: translateX(-26px); }
      23%,38%  { opacity: 1; transform: none; }
      40%,100% { opacity: 0; }
    }
    @keyframes sadBand {
      0%,23%   { background: var(--accent); }
      27%      { background: var(--english-violet); }
      30%      { background: #3f9468; }
      33%      { background: var(--accent); }
      36%,100% { background: var(--english-violet); }
    }
    @keyframes sadFrA {
      0%,40%   { opacity: 0; transform: scale(0.4); }
      43%,57%  { opacity: 1; transform: scale(1); }
      59%,100% { opacity: 0; }
    }
    @keyframes sadFrB {
      0%,43%   { opacity: 0; transform: scale(0.4); }
      46%,57%  { opacity: 1; transform: scale(1); }
      59%,100% { opacity: 0; }
    }
    @keyframes sadFrC {
      0%,46%   { opacity: 0; transform: scale(0.4); }
      49%,57%  { opacity: 1; transform: scale(1); }
      59%,100% { opacity: 0; }
    }
    @keyframes sadStamp {
      0%,62%   { opacity: 0; transform: translate(-50%, -150%) rotate(-7deg) scale(1.1); }
      66%      { opacity: 1; transform: translate(-50%, -8%) rotate(0) scale(1); }
      69%      { transform: translate(-50%, -8%) scale(1.06, 0.92); }
      72%,77%  { opacity: 1; transform: translate(-50%, -8%) scale(1); }
      80%,100% { opacity: 0; }
    }
    @keyframes sadTick {
      0%,70%   { opacity: 0; transform: scale(0.5); }
      74%,77%  { opacity: 1; transform: scale(1); }
      80%,100% { opacity: 0; }
    }
    @keyframes sadPhone {
      0%,80%   { opacity: 0; transform: translateY(14%) scale(0.9); }
      84%,100% { opacity: 1; transform: none; }
    }
    @keyframes sadHeart {
      0%,87%   { opacity: 0; transform: scale(0); }
      90%      { opacity: 1; transform: scale(1.35); }
      92%      { transform: scale(1); }
      94%,100% { opacity: 1; transform: scale(1); }
    }
    @keyframes sadProg { 0% { width: 0; } 100% { width: 100%; } }

    /* Gate playback to on-screen (set by JS); paused until then. */
    .sad-anim { animation-play-state: paused; }
    .sad-stage.is-live .sad-anim { animation-play-state: running; }

    /* Reduced motion / no-JS: a single composed still (scene 1). */
    .sad-stage.is-static .sad-anim { animation: none !important; }
    .sad-stage.is-static .sad-wipe { opacity: 0; }
    .sad-stage.is-static [data-s] { opacity: 0; }
    .sad-stage.is-static [data-s="1"] { opacity: 1; }
    .sad-stage.is-static .sad-progress { display: none; }
  </style>

  <script>
    import { supabase, gbpFromPence } from '../lib/supabase.js';

    const grid = document.getElementById('edit-grid');
    const loading = document.getElementById('edit-loading');
    const empty = document.getElementById('edit-empty');
    const wrap = document.getElementById('edit-catalogue');

    const modal = document.getElementById('checkout');
    const modalClose = document.getElementById('checkout-close');
    const stepPanes = modal.querySelectorAll('.checkout-pane');
    const stepPips = modal.querySelectorAll('.checkout-step');
    const coverEl = document.getElementById('checkout-cover');
    const pkgTitleEl = document.getElementById('checkout-pack-title');
    const pkgDescEl = document.getElementById('checkout-pack-desc');
    const pkgListEl = document.getElementById('checkout-pack-contents');
    const pkgPriceEl = document.getElementById('checkout-pack-price');

    const contactForm = document.getElementById('checkout-contact');
    const paymentForm = document.getElementById('checkout-payment');
    const contactErr = document.getElementById('ck-contact-error');
    const paymentErr = document.getElementById('ck-payment-error');
    const walletBtn = document.getElementById('ck-wallet-btn');
    const cardSubmit = document.getElementById('ck-card-submit');
    const backBtn = document.getElementById('ck-back-to-contact');
    const doneH = document.getElementById('ck-done-h');
    const doneSub = document.getElementById('ck-done-sub');

    const state = {
      pack: null,
      contact: null,
      payMethod: 'apple_pay',
    };

    function escapeHtml(s) {
      return String(s ?? '').replace(/[&<>"']/g, (c) => ({
        '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#39;',
      }[c]));
    }

    function renderShowcase(p, i) {
      const cover = (typeof p.cover_image_url === 'string' && p.cover_image_url) ? p.cover_image_url : '';
      const previews = (p.preview_images ?? []).filter((u) => typeof u === 'string' && u.length > 0);
      // The collage shows the pack's previews; hovering one flickers it into the
      // framed hero. Fall back to the cover if a pack has no separate previews.
      const collageUrls = (previews.length ? previews : (cover ? [cover] : [])).slice(0, 6);
      const thumbs = collageUrls
        .map((url) => `<button type="button" class="show-thumb" data-src="${escapeHtml(url)}" style="background-image:url('${escapeHtml(url)}')" aria-label="Preview"></button>`)
        .join('');

      const isFree = (p.price_pence ?? 0) === 0;
      const priceLabel = isFree ? 'Free' : gbpFromPence(p.price_pence);
      const num = String(i + 1).padStart(2, '0');
      const contentsCount = (p.contents ?? []).length;
      const previewItems = (p.contents ?? []).slice(0, 4);
      const insideList = previewItems.length
        ? `<ul class="show-inside">${previewItems.map((it) => `<li>${escapeHtml(it)}</li>`).join('')}</ul>`
        : '';
      const capBits = [`№ ${num}`];
      if (p.category) capBits.push(escapeHtml(p.category));
      const eyebrow = `${p.category ? escapeHtml(p.category) : 'The Edit'}${contentsCount ? ` · ${contentsCount} posts` : ''}`;

      return `
        <section class="show" data-id="${p.id}" data-slug="${escapeHtml(p.slug)}">
          <div class="show-inner">
            <div class="show-hero">
              <span class="show-ghost" data-par="30" aria-hidden="true">${num}</span>
              <a class="show-stage" href="/edit/${encodeURIComponent(p.slug)}" aria-label="See inside ${escapeHtml(p.title)}">
                <figure class="show-frame">
                  <div class="show-mount">
                    <div class="show-heroimg" data-cover="${escapeHtml(cover)}" style="background-image:url('${escapeHtml(cover)}')"></div>
                  </div>
                  <figcaption class="show-frame-cap">${capBits.join(' · ')}</figcaption>
                </figure>
              </a>
            </div>
            <div class="show-copy">
              <p class="show-eyebrow">${eyebrow}</p>
              <h2 class="show-title">${escapeHtml(p.title)}</h2>
              ${p.description ? `<p class="show-desc">${escapeHtml(p.description)}</p>` : ''}
              ${insideList}
            </div>
            <div class="show-shop">
              <div class="show-collage">${thumbs}</div>
              <div class="show-shopfoot">
                <span class="show-price">${priceLabel}</span>
                <div class="show-ctas">
                  <a class="show-see" href="/edit/${encodeURIComponent(p.slug)}">See inside</a>
                  <button type="button" class="show-buy" data-buy="${p.id}">Buy pack <em>&rarr;</em></button>
                </div>
              </div>
            </div>
          </div>
        </section>
      `;
    }

    // Hovering a collage thumbnail flickers it into the framed hero, and the
    // rest of the collage dims back (CSS). Leaving the collage restores the cover.
    function wireCollage(root) {
      root.querySelectorAll('.show').forEach((sec) => {
        const hero = sec.querySelector('.show-heroimg');
        const collage = sec.querySelector('.show-collage');
        const thumbs = [...sec.querySelectorAll('.show-thumb')];
        if (!hero || !collage || !thumbs.length) return;
        const cover = hero.dataset.cover || '';
        let raf = 0;
        const swap = (url) => {
          if (!url) return;
          hero.style.opacity = '0.18';   // flicker out…
          cancelAnimationFrame(raf);
          raf = requestAnimationFrame(() => setTimeout(() => {
            hero.style.backgroundImage = `url('${url}')`;
            hero.style.opacity = '1';    // …and back in on the new shot
          }, 80));
        };
        thumbs.forEach((t) => {
          const show = () => {
            thumbs.forEach((x) => x.classList.toggle('is-active', x === t));
            swap(t.dataset.src);
          };
          t.addEventListener('mouseenter', show);
          t.addEventListener('focus', show);
        });
        collage.addEventListener('mouseleave', () => {
          thumbs.forEach((x) => x.classList.remove('is-active'));
          swap(cover);
        });
      });
    }

    // Reveal each band on scroll, and gently parallax the big ghost numbers.
    function wireMotion(root) {
      const shows = [...root.querySelectorAll('.show')];
      if (!('IntersectionObserver' in window)) {
        shows.forEach((s) => s.classList.add('is-in'));
      } else {
        const io = new IntersectionObserver((entries) => {
          entries.forEach((e) => {
            if (e.isIntersecting) { e.target.classList.add('is-in'); io.unobserve(e.target); }
          });
        }, { threshold: 0.18, rootMargin: '0px 0px -8% 0px' });
        shows.forEach((s) => io.observe(s));
      }

      const parEls = [...root.querySelectorAll('[data-par]')];
      const reduce = window.matchMedia('(prefers-reduced-motion: reduce)').matches;
      if (parEls.length && !reduce) {
        let ticking = false;
        const update = () => {
          const vh = window.innerHeight || 1;
          parEls.forEach((el) => {
            const r = el.getBoundingClientRect();
            const d = ((r.top + r.height / 2) - vh / 2) / vh;   // -0.5..0.5 across the viewport
            const amp = parseFloat(el.dataset.par) || 0;
            el.style.setProperty('--par', (-d * amp).toFixed(1) + 'px');
          });
          ticking = false;
        };
        window.addEventListener('scroll', () => {
          if (!ticking) { ticking = true; requestAnimationFrame(update); }
        }, { passive: true });
        update();
      }
    }

    // ===== Hero "film" =====================================================
    // Auto-plays a ~15s title sequence: template stills flip into a 4×4 grid in
    // a random order, keep firing through, settle on a full grid, then the whole
    // grid fades to empty — and loops. Pool = every image across the packs.
    function initHeroFilm(pool) {
      const grid = document.getElementById('eh-grid');
      if (!grid) return;
      pool = [...new Set((pool || []).filter((u) => typeof u === 'string' && u.length))];
      if (!pool.length) return;

      let tiles = [...grid.querySelectorAll('.eh-tile')];
      if (!tiles.length) {
        grid.innerHTML = Array.from({ length: 16 }, () => '<div class="eh-tile"><div class="eh-tile-img"></div></div>').join('');
        tiles = [...grid.querySelectorAll('.eh-tile')];
      }

      const shuffle = (a) => { a = a.slice(); for (let i = a.length - 1; i > 0; i--) { const j = Math.floor(Math.random() * (i + 1)); [a[i], a[j]] = [a[j], a[i]]; } return a; };
      const reduce = window.matchMedia('(prefers-reduced-motion: reduce)').matches;

      if (reduce) {
        const imgs = shuffle(pool);
        tiles.forEach((t, i) => { t.classList.add('is-on'); t.querySelector('.eh-tile-img').style.backgroundImage = `url('${imgs[i % imgs.length]}')`; });
        return;
      }

      // Preload so flips don't show empty cells on first pass.
      pool.forEach((u) => { const im = new Image(); im.src = u; });

      let timers = [];
      let bag = [];
      const nextImg = () => { if (!bag.length) bag = shuffle(pool); return bag.pop(); };
      const setTile = (t, url) => {
        const img = t.querySelector('.eh-tile-img');
        img.style.backgroundImage = `url('${url}')`;
        t.classList.add('is-on');
        t.classList.remove('is-pop'); void t.offsetWidth; t.classList.add('is-pop');
      };

      const FILL_START = 500, FILL_END = 10000, FADE_AT = 13000, LOOP_AT = 15400;

      function run() {
        timers.forEach(clearTimeout); timers = [];
        grid.classList.remove('is-fading');
        tiles.forEach((t) => { t.classList.remove('is-on', 'is-pop'); t.querySelector('.eh-tile-img').style.backgroundImage = ''; });
        bag = [];

        // 1) reveal each cell once, in a random order, accelerating to the end.
        const order = shuffle(tiles.map((_, i) => i));
        order.forEach((cellIdx, n) => {
          const frac = order.length > 1 ? n / (order.length - 1) : 0;
          const at = n === 0 ? FILL_START : FILL_START + 600 + Math.pow(frac, 1.5) * (FILL_END - FILL_START - 600);
          timers.push(setTimeout(() => setTile(tiles[cellIdx], nextImg()), at));
        });

        // 2) "fire through" — keep re-flipping already-revealed cells to new stills.
        for (let at = 2400; at < FILL_END; at += 140 + Math.random() * 170) {
          const when = at;
          timers.push(setTimeout(() => {
            const on = tiles.filter((t) => t.classList.contains('is-on'));
            if (on.length) setTile(on[Math.floor(Math.random() * on.length)], nextImg());
          }, when));
        }

        // 3) hold the full grid → fade to empty → loop.
        timers.push(setTimeout(() => grid.classList.add('is-fading'), FADE_AT));
        timers.push(setTimeout(run, LOOP_AT));
      }

      // Kick off once the hero is on screen (it's at the top, so ~immediately).
      if ('IntersectionObserver' in window) {
        let started = false;
        const io = new IntersectionObserver((entries) => {
          entries.forEach((e) => { if (e.isIntersecting && !started) { started = true; run(); io.disconnect(); } });
        }, { threshold: 0.2 });
        io.observe(grid);
      } else { run(); }
    }

    let packsById = new Map();

    // Dev/preview-only sample packs so the showcase is visible before any packs
    // exist in Supabase. Never shown in production (real packs or the empty
    // state). Images are local placeholders from /public/assets.
    const SAMPLE_PACKS = [
      {
        id: 'sample-new-instruction', slug: 'new-instruction', category: 'Listings',
        title: 'New Instruction', price_pence: 29900,
        description: 'The full launch moment for a fresh listing — coming-soon teasers, a just-listed carousel and matching story covers that make every new instruction feel like an event.',
        cover_image_url: '/assets/home%20kitchen.jpg',
        preview_images: ['/assets/cosy%20home.jpg', '/assets/sitting%20chair.jpg', '/assets/home%20chair.jpg', '/assets/home%20window.jpg', '/assets/Doors.jpg', '/assets/outside.jpg'],
        contents: ['9 feed posts', '6 story covers', 'Just-listed carousel', 'Coming-soon teaser'],
      },
      {
        id: 'sample-just-sold', slug: 'just-sold', category: 'Social Proof',
        title: 'Just Sold', price_pence: 24900,
        description: 'Turn every completion into content. A celebratory “sold” set with testimonial frames and before/after layouts that keep your pipeline warm.',
        cover_image_url: '/assets/Home-window.jpg',
        preview_images: ['/assets/home%20chair.jpg', '/assets/Doors.jpg', '/assets/doors-open.png', '/assets/home%20forest.jpg', '/assets/cosy%20home.jpg', '/assets/sitting%20chair.jpg'],
        contents: ['8 feed posts', 'Testimonial frames', 'Before / after layout', 'Sold-board story'],
      },
      {
        id: 'sample-market-update', slug: 'market-update', category: 'Authority',
        title: 'Market Update', price_pence: 19900,
        description: 'Position yourself as the local expert — clean, data-led templates for monthly market notes, tips and “ask the agent” prompts.',
        cover_image_url: '/assets/outside.jpg',
        preview_images: ['/assets/home%20forest.jpg', '/assets/tea.jpg', '/assets/home%20deer.jpg', '/assets/home%20ginger.jpg', '/assets/vibes.jpg', '/assets/cosy%20home.jpg'],
        contents: ['6 feed posts', 'Stat / chart frames', 'Tip-of-the-week set', 'Q&A story template'],
      },
      {
        id: 'sample-meet-the-team', slug: 'meet-the-team', category: 'Brand',
        title: 'Meet the Team', price_pence: 17900,
        description: 'Put faces to the name. A warm personal-brand set for team intros, day-in-the-life stories and the human side of your agency.',
        cover_image_url: '/assets/social%20media.jpg',
        preview_images: ['/assets/Danni.png', '/assets/Abbie.png', '/assets/June1.png', '/assets/abbie%202.png', '/assets/Danni%202.png', '/assets/vibes.jpg'],
        contents: ['7 feed posts', 'Team intro frames', 'Day-in-the-life story', 'Quote cards'],
      },
    ];

    (async () => {
      const { data, error } = await supabase
        .from('packs')
        .select('id, slug, title, description, price_pence, cover_image_url, preview_images, contents, category, sort_order')
        .eq('status', 'active')
        .order('sort_order', { ascending: true })
        .order('created_at', { ascending: false })
        .limit(8);

      loading.hidden = true;
      wrap.setAttribute('aria-busy', 'false');

      let packs = (!error && data) ? data : [];
      // Show the sample showcase locally when there's no live data yet.
      if (packs.length === 0 && import.meta.env.DEV) packs = SAMPLE_PACKS;

      if (error && !import.meta.env.DEV) {
        empty.hidden = false;
        empty.innerHTML = `<p>Could not load the catalogue right now — please try again shortly.</p>`;
        return;
      }
      if (packs.length === 0) { empty.hidden = false; return; }

      packsById = new Map(packs.map((p) => [p.id, p]));
      grid.innerHTML = packs.map((p, i) => renderShowcase(p, i)).join('');
      wireCollage(grid);
      wireMotion(grid);
      maybeOpenFromQuery();

      // Feed the hero "film" every image across the packs.
      initHeroFilm(packs.flatMap((p) => [p.cover_image_url, ...(p.preview_images ?? [])]));
    })();

    // ============== Modal helpers ==============
    function showStep(step) {
      stepPanes.forEach((p) => p.classList.toggle('is-active', p.dataset.step === step));
      const order = ['contact', 'payment', 'done'];
      const idx = order.indexOf(step);
      stepPips.forEach((pip) => {
        const i = order.indexOf(pip.dataset.stepPip);
        pip.classList.toggle('is-active', i === idx);
        pip.classList.toggle('is-done', i < idx);
      });
    }

    function openCheckout(pack) {
      state.pack = pack;
      state.contact = null;
      state.payMethod = 'apple_pay';

      pkgTitleEl.textContent = pack.title;
      pkgDescEl.textContent = pack.description ?? '';
      pkgListEl.innerHTML = (pack.contents ?? []).slice(0, 4).map((c) => `<li>${escapeHtml(c)}</li>`).join('');
      pkgPriceEl.textContent = gbpFromPence(pack.price_pence);
      coverEl.style.backgroundImage = pack.cover_image_url ? `url('${pack.cover_image_url}')` : '';

      // Update £ amount on payment step in two places
      modal.querySelectorAll('[data-bind="amount"]').forEach((el) => {
        el.textContent = gbpFromPence(pack.price_pence);
      });

      // Reset form state
      contactForm.reset();
      paymentForm.reset();
      contactErr.hidden = true; paymentErr.hidden = true;
      selectPayMethod('apple_pay');

      showStep('contact');
      modal.hidden = false;
      document.body.style.overflow = 'hidden';
      setTimeout(() => contactForm.elements.name.focus(), 60);
    }

    function closeCheckout() {
      modal.hidden = true;
      document.body.style.overflow = '';
    }

    grid.addEventListener('click', (e) => {
      const target = e.target;
      if (!(target instanceof HTMLElement)) return;
      // Only the explicit Buy button opens checkout. Clicking elsewhere on
      // the card follows the wrapping <a> to the pack-detail page.
      const buyEl = target.closest('[data-buy]');
      if (!buyEl) return;
      e.preventDefault();
      const id = buyEl.getAttribute('data-buy');
      if (!id) return;
      const pack = packsById.get(id);
      if (pack) openCheckout(pack);
    });

    // ?buy=<id> deep-link: pack-detail page sends users back here to
    // trigger the checkout modal for a specific pack.
    function maybeOpenFromQuery() {
      const want = new URLSearchParams(location.search).get('buy');
      if (!want) return;
      const pack = packsById.get(want);
      if (pack) {
        openCheckout(pack);
        // Clean the URL so refreshes don't re-open the modal
        const u = new URL(location.href);
        u.searchParams.delete('buy');
        history.replaceState({}, '', u.toString());
      }
    }

    modalClose.addEventListener('click', closeCheckout);
    modal.addEventListener('click', (e) => { if (e.target === modal) closeCheckout(); });
    document.addEventListener('keydown', (e) => {
      if (e.key === 'Escape' && !modal.hidden) closeCheckout();
    });

    // ============== Step 1: contact ==============
    contactForm.addEventListener('submit', (e) => {
      e.preventDefault();
      contactErr.hidden = true;
      const fd = new FormData(contactForm);
      const name = String(fd.get('name') || '').trim();
      const email = String(fd.get('email') || '').trim();
      const phone = String(fd.get('phone') || '').trim();
      const company = String(fd.get('company') || '').trim();
      if (!name || !email || !phone || !company) {
        contactErr.textContent = 'Please complete every field so we can email your library link.';
        contactErr.hidden = false;
        return;
      }
      if (!/^[^@\s]+@[^@\s]+\.[^@\s]+$/.test(email)) {
        contactErr.textContent = 'That email doesn’t look quite right — double-check it?';
        contactErr.hidden = false;
        return;
      }
      state.contact = { name, email, phone, company };
      showStep('payment');
    });

    // ============== Step 2: payment ==============
    function selectPayMethod(method) {
      state.payMethod = method;
      modal.querySelectorAll('.ck-paytab').forEach((tab) => {
        const active = tab.dataset.pay === method;
        tab.classList.toggle('is-active', active);
        tab.setAttribute('aria-selected', String(active));
      });
      const wallet = modal.querySelector('[data-paypane="wallet"]');
      const card = modal.querySelector('[data-paypane="card"]');
      const isWallet = method !== 'card';
      wallet.hidden = !isWallet;
      card.hidden = isWallet;
      if (isWallet) {
        const label = walletBtn.querySelector('.ck-wallet-btn-label');
        label.innerHTML = method === 'apple_pay'
          ? 'Pay with <strong> Pay</strong>'
          : 'Pay with <strong>G Pay</strong>';
        walletBtn.dataset.wallet = method;
      }
    }

    modal.querySelectorAll('.ck-paytab').forEach((tab) => {
      tab.addEventListener('click', () => selectPayMethod(tab.dataset.pay));
    });

    backBtn.addEventListener('click', () => showStep('contact'));

    walletBtn.addEventListener('click', () => processPayment());

    paymentForm.addEventListener('submit', (e) => {
      e.preventDefault();
      if (state.payMethod !== 'card') { processPayment(); return; }
      paymentErr.hidden = true;
      const fd = new FormData(paymentForm);
      const num = String(fd.get('card_number') || '').replace(/\s+/g, '');
      const exp = String(fd.get('card_exp') || '').trim();
      const cvc = String(fd.get('card_cvc') || '').trim();
      const postcode = String(fd.get('card_postcode') || '').trim();
      if (num.length < 12 || exp.length < 4 || cvc.length < 3 || !postcode) {
        paymentErr.textContent = 'Please complete your card details before paying.';
        paymentErr.hidden = false;
        return;
      }
      processPayment();
    });

    // ============== Step 3: processing → redirect ==============
    async function processPayment() {
      showStep('done');
      doneH.textContent = state.payMethod === 'card' ? 'Charging your card…' : 'Authenticating with your wallet…';
      doneSub.textContent = "This usually takes a few seconds. Please don't refresh.";

      // STUB: simulate the network round-trip. Real Stripe wiring lives behind
      // a Supabase edge function that exchanges this for a PaymentIntent.
      await new Promise((r) => setTimeout(r, 1400));

      const { pack, contact, payMethod } = state;
      const payment_ref = 'stub_' + Math.random().toString(36).slice(2, 12);

      const { data, error } = await supabase
        .from('orders')
        .insert({
          pack_id: pack.id,
          pack_slug: pack.slug,
          pack_title: pack.title,
          amount_pence: pack.price_pence,
          buyer_name: contact.name,
          buyer_email: contact.email,
          buyer_phone: contact.phone,
          buyer_company: contact.company,
          payment_method: payMethod,
          payment_ref,
          status: 'paid',
        })
        .select('id, invoice_number')
        .single();

      if (error || !data) {
        doneH.textContent = 'Something went wrong';
        doneSub.textContent = error?.message || 'Could not record the order. Please try again.';
        return;
      }

      // Stash a temporary handle so /edit/thanks can show the receipt while
      // also being reachable via the email link (?order=ID).
      try {
        sessionStorage.setItem('tmke.lastOrder', JSON.stringify({
          id: data.id,
          invoice_number: data.invoice_number,
          email: contact.email,
        }));
      } catch (_) { /* ignore */ }

      window.location.href = `/edit/thanks?order=${encodeURIComponent(data.id)}`;
    }

  </script>

  <script>
    // Studio demo: only run the loop while it's on screen (perf), and fall
    // back to a composed still for visitors who prefer reduced motion.
    (() => {
      const fig = document.querySelector('[data-studio-demo]');
      if (!fig) return;
      if (window.matchMedia('(prefers-reduced-motion: reduce)').matches) {
        fig.classList.add('is-static');
        return;
      }
      if (!('IntersectionObserver' in window)) { fig.classList.add('is-live'); return; }
      const io = new IntersectionObserver((entries) => {
        entries.forEach((e) => fig.classList.toggle('is-live', e.isIntersecting));
      }, { threshold: 0.2 });
      io.observe(fig);
    })();
  </script>
</BaseLayout>
