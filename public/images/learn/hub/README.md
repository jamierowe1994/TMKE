# Hub tour screenshots — "Getting around your hub" course

Drop screenshots here with these **exact filenames**. Each appears automatically
on its slide in the course (`/account/guides/read?g=getting-around-your-hub`).
Until a file exists, that slide shows a clean tinted placeholder — nothing breaks.

Recommended: **~1600×1000px (16:10), PNG or JPG**. A wide crop of the top of each
page works best. Use your real hub so it shows real designs/orders, not empty states.

| File            | Screenshot of…                                    |
|-----------------|---------------------------------------------------|
| `dashboard.png` | The Dashboard (home landing)                       |
| `studio.png`    | The Studio — canvas + "Your designs"               |
| `shop.png`      | The Shop / The Edit — pack catalogue               |
| `planner.png`   | The Planner — content calendar                     |
| `orders.png`    | Orders — purchase history                          |
| `bookings.png`  | Bookings — upcoming + a booking card               |
| `brand-kit.png` | Brand Kit (in Profile) — colours/fonts/logo        |
| `learn.png`     | The Learn section — guides grid                    |

Optional: `cover.png` for the course's opening/overview slide (set `cover_url`
to `/images/learn/hub/cover.png` in `/admin/guides` if you add one).

The frames are 16:10; anything close is fine — they cover-fit and centre-crop.
