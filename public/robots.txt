# tmke.co.uk
#
# Everything public is open to crawlers. The member hub (/account/), the admin
# centre (/admin/) and the delivery pages (/deliver/) carry a "noindex" tag of
# their own instead of being blocked here - deliberately. A page blocked in this
# file is never fetched, so a crawler never sees its noindex, and any URL it
# already knows about can sit in the results as a bare link. Letting it read the
# page and be told "don't index this" is what actually gets them dropped.

User-agent: *
Allow: /

Sitemap: https://tmke.co.uk/sitemap-index.xml
